# Insight 360 Blueprint v3.2

**Version:** 3.2
**Date:** January 11, 2026
**Status:** Implementation | Bug Fixes & Align 120 Redesign Planning
**Codename:** Governance
**Previous Version:** v3.1 (Navigation & Modal Fixes)

---

## Executive Summary

Insight 360 v3.2 delivers critical P1 production bug fixes and introduces the comprehensive Align 120 Redesign Plan. This release resolves the S2E "Save & Continue" UUID error, Context Asset deletion issues, and implements RLS policy fixes across 11 tables. The Align 120 Redesign Plan establishes a new architecture for AI agent integration across all 5 modules with data flow to DIGM, Strategy 120, Parthenon OKRs, and Integrity Dashboard.

**Key Achievements:**
- Fixed S2E null UUID error preventing foundation creation
- Comprehensive RLS policy fixes for all affected tables
- Align 120 Redesign Plan with Modal Dialog Service architecture
- Documentation reorganization (Bug Fixes, Roadmap, design folders)

---

## What's Fixed in v3.2

### P1 Bug Fixes

| Bug | Root Cause | Solution |
|-----|------------|----------|
| S2E "Save & Continue" null UUID | `setCurrentFoundation` called with null foundationId | Added early return for null foundationId, added user_id filtering |
| Button hover disappearing | CSS `var(--primary-hover)` undefined | Changed to `var(--primary-dark, #4f46e5)` with fallback |
| Auth returning null in dev | getUserId not falling back to DEFAULT_USER_ID | Added development fallback for DEFAULT_USER_ID |
| RLS policy violations | Policies blocking server operations | Created comprehensive RLS fix SQL with service role bypass |

### S2E Service Fix (s2eService.js)

```javascript
async function setCurrentFoundation(userId, foundationId) {
    // Unset any existing current foundation for this user
    await supabase
        .from('strategic_foundations')
        .update({ is_current: false })
        .eq('user_id', userId)
        .eq('is_current', true);

    // If no foundationId provided, just unset existing (used before creating new)
    if (!foundationId) {
        return null;
    }

    // Set the new foundation as current
    const { data, error } = await supabase
        .from('strategic_foundations')
        .update({ is_current: true })
        .eq('id', foundationId)
        .eq('user_id', userId)
        .select()
        .single();

    if (error) throw error;
    return data;
}
```

### Auth Utility Enhancement (auth.js)

```javascript
function getUserId(req) {
    const userId = req.userId || req.user?.id;
    const isValidUserId = userId && userId !== 'anonymous' && userId !== 'null';

    // Development fallback to prevent null UUID errors
    if (!isValidUserId && process.env.NODE_ENV !== 'production') {
        return process.env.DEFAULT_USER_ID || process.env.DEV_USER_ID || null;
    }

    return isValidUserId ? userId : null;
}
```

---

## Align 120 Redesign Plan

### Architecture Overview

The redesigned Align 120 module integrates specialized AI agents for each step, with outputs flowing to downstream systems:

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           ALIGN 120 REDESIGN                                 │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  Module 1: AI Assessment Agent → Service recommendations                    │
│  Module 2: Values/Vision/Mission Agents → DIGM, Strategy 120, OKRs         │
│  Module 3: Training Needs Agent → Role-specific AI training                 │
│  Module 4: Brand Analysis Agent (web scraper) → BrandVoiceDNA, ICP         │
│  Module 5: Change Management Agent → Change program recommendations         │
│                                                                              │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │                    MODAL DIALOG SERVICE                              │    │
│  │  - Agent execution UI       - Progress tracking                      │    │
│  │  - Workflow orchestration   - Result display                         │    │
│  │  - Interactive Q&A          - Data validation                        │    │
│  └─────────────────────────────────────────────────────────────────────┘    │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│                          DATA FLOW DESTINATIONS                              │
├──────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  ┌──────────┐   ┌──────────────┐   ┌──────────────┐   ┌─────────────────┐   │
│  │   DIGM   │   │ Strategy 120 │   │   Parthenon  │   │   Integrity     │   │
│  │ 4 Layers │   │     S2E      │   │     OKRs     │   │   Dashboard     │   │
│  └──────────┘   └──────────────┘   └──────────────┘   └─────────────────┘   │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘
```

### Modal Dialog Service

New reusable service for running agents/workflows:

| Feature | Description |
|---------|-------------|
| Agent Execution | Run single agent with progress tracking |
| Workflow Orchestration | Run multiple agents in sequence |
| Streaming Response | Display live agent output |
| Result Actions | Accept, Edit, or Regenerate outputs |
| Context Assembly | Auto-inject session/company context |

### Data Flow Integration

| Align 120 Module | Output | Destination |
|-----------------|--------|-------------|
| Module 1: AI Assessment | risk_register | Integrity Dashboard (alerts) |
| Module 1: AI Assessment | opportunities | Strategy 120 (initiatives) |
| Module 2: Values/Vision/Mission | core_values | DIGM Identity Layer |
| Module 2: Values/Vision/Mission | vision/mission | Strategy 120 (foundation) |
| Module 2: Values/Vision/Mission | preliminary_okrs | Parthenon (company OKRs) |
| Module 3: Training Needs | skills_matrix | Parthenon (roles) |
| Module 3: Training Needs | change_readiness | DIGM Adaptation Layer |
| Module 4: Brand Analysis | brand_voice_dna | DIGM Voice Layer |
| Module 4: Brand Analysis | ideal_customer_profile | Strategy 120 (customer perspective) |
| Module 5: Change Management | governance_charter | DIGM Cognitive Layer |
| Module 5: Change Management | raci_matrix | Parthenon (OKR ownership) |
| Module 5: Change Management | success_metrics | Integrity Dashboard (metrics) |

---

## Files Changed

### Modified Files

| File | Changes |
|------|---------|
| `server/services/s2eService.js` | Fixed `setCurrentFoundation` null UUID handling, added user_id filtering |
| `server/utils/auth.js` | Added DEFAULT_USER_ID fallback for development |
| `public/strategy.html` | Fixed button hover CSS, panel flexbox layout |
| `public/context.html` | Context asset UI improvements |
| `public/js/context.js` | Context asset functionality |
| `server/routes/context.js` | Context route fixes |

### New Files

| File | Purpose |
|------|---------|
| `db/phase3.2-fix-all-rls-bugs.sql` | Comprehensive RLS policy fixes for 11 tables |
| `scripts/check-production-config.js` | Config validation script |
| `scripts/deploy-rls-fixes.sh` | Deployment helper script |
| `documentation/design/PLAN-align120-redesign.md` | Full Align 120 redesign architecture |
| `documentation/Bug Fixes/PRODUCTION_BUG_FIXES.md` | Bug tracking documentation |
| `documentation/Bug Fixes/RLS_FIXES_APPLIED.md` | RLS fix documentation |

### Documentation Reorganization

| Folder | Contents |
|--------|----------|
| `documentation/Bug Fixes/` | Production bug tracking docs |
| `documentation/Roadmap/` | Versioned roadmap files |
| `documentation/design/` | Design documents including Align 120 plan |

---

## RLS Policy Fixes

### Tables Updated (11 total)

1. `align120_sessions` - Align 120 session management
2. `company_profiles` - Company profile storage
3. `ai_maturity_assessments` - Module 1 outputs
4. `business_fundamentals` - Module 2 outputs
5. `team_readiness_assessments` - Module 3 outputs
6. `brand_alignment_assessments` - Module 4 outputs
7. `corporate_alignments` - Module 5 outputs
8. `strategic_foundations` - S2E foundation data
9. `bsc_perspectives` - Balanced scorecard
10. `context_assets` - Context asset storage
11. `conversations` - Multi-LLM chat history

### Policy Structure (per table)

| Policy | Purpose |
|--------|---------|
| Service Role Bypass | `auth.role() = 'service_role'` - Full access for server |
| View Own Data | `auth.uid() = user_id` - Users see their own records |
| Insert Own/Service | User or service can insert |
| Update Own | Users update their own records |
| Delete Own | Users delete their own records |

---

## Testing Completed

| Feature | Status | Notes |
|---------|--------|-------|
| S2E Save & Continue | ✅ | No more null UUID errors |
| Context Asset Deletion | ✅ | Hard delete working |
| Parthenon Department Creation | ✅ | RLS allowing inserts |
| Align 120 Session Creation | ✅ | Session creation successful |
| Button Hover Effect | ✅ | CSS fallback working |

---

## Commits

| Hash | Message |
|------|---------|
| `3b94daf` | Fix S2E UUID bug, reorganize docs, add Align 120 redesign plan |
| `3422ebe` | Fix 5 P1 production bugs and reorganize navigation |

---

## Next Steps (v3.3+)

### Phase 1: Modal Dialog Service
- Create `agent-dialog-service.js` frontend service
- Create `agent-dialog.css` modal styling
- Create `agentExecutionService.js` backend orchestration

### Phase 2-6: Align 120 Module Implementation
- Module 1: AI Assessment Agent
- Module 2: Values/Vision/Mission Agents
- Module 3: Training Needs Agent
- Module 4: Brand Analysis Agent (web scraper)
- Module 5: Change Management Agent

### Phase 7: Integration & Testing
- Create `align120IntegrationService.js`
- Implement all downstream sync functions
- Generate Alignment Brief
- End-to-end testing

---

## Version History

| Version | Date | Focus |
|---------|------|-------|
| v3.0 | 2026-01-09 | SynergiNexus & Role-Based Architecture |
| v3.1 | 2026-01-09 | Navigation & Modal Fixes |
| v3.2 | 2026-01-11 | Bug Fixes & Align 120 Redesign Planning |

# Insight 360 Blueprint v3.22

**Version:** 3.22
**Date:** January 21, 2026
**Status:** Current | Agency Foundation & Connection Management
**Codename:** Constellation
**Previous Version:** v3.21 (Admin Conversations & Quick Prompts)
**Latest Update:** Phase 39-40: Agency Foundation & Connection Management Infrastructure

---

## Executive Summary

Insight 360 is a **Values-Based AI Ecosystem** — a personal AI command center that aligns automated systems with organizational ethics. Version 3.22 delivers **Phase 39: Agency Foundation** and **Phase 40: Connection Management Infrastructure**, enabling multi-tenant agency support and comprehensive organizational-to-AI capability mapping.

**Core Philosophy:** *"Technology should augment human brilliance—not replace it."*

---

## Phase 39: Agency Foundation (v3.22)

### Feature 1: Multi-Tenant Organization Support

Complete infrastructure for agencies managing multiple client organizations.

| Component | Description |
|-----------|-------------|
| **Organizations Table** | Multi-tenant organization support with subscription tiers |
| **Organization Members** | Role-based team membership (owner, admin, consultant, viewer) |
| **Clients Table** | Client management per organization |
| **Org-Aware RLS** | Row Level Security policies for data isolation |

### Feature 2: Agency Admin Pages

New administrative interfaces for agency management.

| Page | Purpose |
|------|---------|
| `admin-org-settings.html` | Organization settings, branding, subscription info |
| `admin-org-members.html` | Invite and manage team members |
| `admin-clients.html` | Create and manage client records |

### Feature 3: Client Context Integration

Client selection integrated into key workflows.

| Integration | Description |
|-------------|-------------|
| **Company Dashboard** | Client selector for agency consultants |
| **Align 120** | Client context in assessment sessions |
| **Client Comparison** | Cross-client analytics view |

### New Database Tables (Phase 39)

```sql
organizations         -- Multi-tenant orgs with subscription tiers
organization_members  -- User-org membership with roles
clients              -- Client records per organization
```

### New API Endpoints (Phase 39)

| Endpoint | Method | Description |
|----------|--------|-------------|
| `GET /api/organizations` | GET | List user's organizations |
| `POST /api/organizations` | POST | Create new organization |
| `GET /api/organizations/:id` | GET | Get organization details |
| `PUT /api/organizations/:id` | PUT | Update organization |
| `DELETE /api/organizations/:id` | DELETE | Delete organization |
| `GET /api/organizations/:id/stats` | GET | Organization statistics |
| `GET /api/org-members/:orgId` | GET | List organization members |
| `POST /api/org-members/:orgId/invite` | POST | Invite new member |
| `PUT /api/org-members/:orgId/:memberId` | PUT | Update member role |
| `DELETE /api/org-members/:orgId/:memberId` | DELETE | Remove member |
| `GET /api/clients` | GET | List clients (with org_id filter) |
| `POST /api/clients` | POST | Create new client |
| `GET /api/clients/:id` | GET | Get client details |
| `PUT /api/clients/:id` | PUT | Update client |
| `DELETE /api/clients/:id` | DELETE | Archive client |

---

## Phase 40: Connection Management Infrastructure (v3.22)

### Feature 1: Skill Access Control

Role-based access control for skills, matching the pattern used for agents and workflows.

| Table | Purpose |
|-------|---------|
| `skill_roles` | Role-level skill access (view/execute permissions) |
| `skill_tags` | Tag-based skill matching |
| `skill_okr_mappings` | Skills to OKRs connection tracking |

### Feature 2: Responsibility-AI Mappings

Direct connections between organizational responsibilities and AI capabilities.

| Table | Purpose |
|-------|---------|
| `responsibility_agents` | Agents recommended per responsibility |
| `responsibility_skills` | Skills recommended per responsibility |
| `responsibility_workflows` | Workflows recommended per responsibility |

### Feature 3: User Override System

Individual user capability assignments that override role-based access.

| Table | Purpose |
|-------|---------|
| `user_ai_assignments` | Grant/revoke/feature capabilities per user |

### Feature 4: Unified Capability Views

Database views aggregating all access paths for efficient querying.

| View | Purpose |
|------|---------|
| `user_full_capabilities` | All capabilities accessible to a user |
| `okr_supporting_capabilities` | Capabilities supporting an OKR |
| `responsibility_ai_recommendations` | AI recommendations per responsibility |

### Feature 5: Connection Management Pages

New administrative interfaces for managing organizational-to-AI connections.

| Page | Purpose |
|------|---------|
| `my-capabilities.html` | User dashboard showing accessible AI tools |
| `admin-responsibilities.html` | List and manage responsibilities |
| `admin-responsibility-ai.html` | Configure AI recommendations per responsibility |
| `admin-department-ai.html` | Department-level AI configuration |
| `admin-okr-capabilities.html` | View/manage OKR-supporting capabilities |
| `client-comparison.html` | Cross-client comparison for agencies |

### New API Endpoints (Phase 40)

| Endpoint | Method | Description |
|----------|--------|-------------|
| `GET /api/connections/capabilities` | GET | Current user's accessible capabilities |
| `GET /api/connections/responsibilities/:id/ai` | GET | AI recommendations for responsibility |
| `POST /api/connections/responsibilities/:id/agents` | POST | Add agent to responsibility |
| `POST /api/connections/responsibilities/:id/skills` | POST | Add skill to responsibility |
| `POST /api/connections/responsibilities/:id/workflows` | POST | Add workflow to responsibility |
| `DELETE /api/connections/responsibilities/:id/agents/:agentId` | DELETE | Remove agent |
| `DELETE /api/connections/responsibilities/:id/skills/:skillId` | DELETE | Remove skill |
| `DELETE /api/connections/responsibilities/:id/workflows/:workflowId` | DELETE | Remove workflow |
| `GET /api/connections/skills/:id/okrs` | GET | OKRs mapped to skill |
| `POST /api/connections/skills/:id/okrs` | POST | Map skill to OKR |
| `DELETE /api/connections/skills/:skillId/okrs/:okrId` | DELETE | Remove mapping |
| `GET /api/connections/okrs/:id/capabilities` | GET | Capabilities supporting OKR |
| `GET /api/connections/users/:id/assignments` | GET | User's individual overrides |
| `POST /api/connections/users/:id/assignments` | POST | Add user assignment |
| `DELETE /api/connections/users/:userId/assignments/:assignmentId` | DELETE | Remove assignment |
| `GET /api/user-profile` | GET | Current user's profile |

---

## Files Created/Modified

### New Files

| File | Purpose |
|------|---------|
| `db/phase39-agency-foundation.sql` | Organizations, members, clients tables |
| `db/phase39-rls-org-aware.sql` | Org-aware RLS policies |
| `db/phase40-connection-management.sql` | Connection management tables/views |
| `server/routes/organizations.js` | Organizations API |
| `server/routes/org-members.js` | Organization members API |
| `server/routes/clients.js` | Clients API |
| `server/routes/connections.js` | Connection management API |
| `public/admin-org-settings.html` | Organization settings page |
| `public/admin-org-members.html` | Team management page |
| `public/admin-clients.html` | Client management page |
| `public/my-capabilities.html` | User capabilities dashboard |
| `public/admin-responsibilities.html` | Responsibilities management |
| `public/admin-responsibility-ai.html` | Responsibility AI mapping |
| `public/admin-department-ai.html` | Department AI configuration |
| `public/admin-okr-capabilities.html` | OKR capabilities view |
| `public/client-comparison.html` | Client comparison page |
| `public/js/org-switcher.js` | Organization selector component |
| `public/js/client-selector.js` | Client selector component |
| `documentation/guides/my-capabilities-user-guide.md` | User guide |
| `documentation/guides/admin-responsibilities-user-guide.md` | User guide |
| `documentation/guides/admin-responsibility-ai-user-guide.md` | User guide |
| `documentation/guides/admin-department-ai-user-guide.md` | User guide |
| `documentation/guides/admin-okr-capabilities-user-guide.md` | User guide |
| `documentation/guides/client-comparison-user-guide.md` | User guide |
| `documentation/guides/connection-management-technical-guide.md` | Technical guide |

### Modified Files

| File | Change |
|------|--------|
| `server/index.js` | Registered new routes |
| `server/routes/user-profile.js` | Added GET `/` for current user |
| `public/administrator.html` | Added Connection Management section |
| `public/js/help-registry.js` | Added Phase 40 page entries |
| `public/company-dashboard.html` | Added client selector integration |
| `public/align120.html` | Added client context support |

---

## Technical Architecture

### Authentication Pattern

All agency pages use `authFetch` helper for authenticated API calls:

```javascript
function getAuthToken() {
    const token = localStorage.getItem('insight360_token') || localStorage.getItem('auth_token');
    if (token) return token;

    const cookies = document.cookie.split(';');
    for (const cookie of cookies) {
        const [name, value] = cookie.trim().split('=');
        if (name === 'auth_token') return value;
    }
    return null;
}

async function authFetch(url, options = {}) {
    const token = getAuthToken();
    const headers = {
        'Content-Type': 'application/json',
        ...(options.headers || {}),
        ...(token ? { 'Authorization': `Bearer ${token}` } : {})
    };
    return fetch(url, { ...options, headers });
}
```

### Page Structure Pattern

All new pages follow app-layout structure with sidebar navigation:

```html
<div class="app-layout">
    <aside class="sidebar"></aside>
    <main class="main-content">
        <!-- Page content -->
    </main>
</div>
```

### Help Integration

All pages include help modal support with registry entries.

---

## Connection Flow Architecture

```
DEPARTMENT
    │
    ├── department_roles (job titles)
    │       │
    │       └── role_responsibilities
    │               │
    │               ├── responsibility_agents
    │               ├── responsibility_skills
    │               └── responsibility_workflows
    │
    ├── OKRs (department scope)
    │       │
    │       ├── action_okrs
    │       ├── workflow_okr_mappings
    │       └── skill_okr_mappings
    │
    └── AI Capabilities
            ├── agents → agent_roles, department_agents
            ├── skills → skill_roles, skill_tags
            └── workflows → workflow_roles

USER
    │
    ├── user_roles → inherits role's responsibilities
    ├── business_role → inherits role-level access
    └── user_ai_assignments → individual overrides
```

---

## Migration Notes

### Running Phase 39 Schema

```sql
-- Run in Supabase SQL Editor
-- 1. Run db/phase39-agency-foundation.sql
-- 2. Run db/phase39-rls-org-aware.sql

-- Verify tables
SELECT table_name FROM information_schema.tables
WHERE table_schema = 'public'
AND table_name IN ('organizations', 'organization_members', 'clients');
```

### Running Phase 40 Schema

```sql
-- Run in Supabase SQL Editor
-- Run db/phase40-connection-management.sql

-- Verify tables
SELECT table_name FROM information_schema.tables
WHERE table_schema = 'public'
AND table_name LIKE '%responsibility_%' OR table_name LIKE '%skill_%';
```

---

## Version History

| Version | Date | Codename | Description |
|---------|------|----------|-------------|
| 3.22 | 2026-01-21 | Constellation | Phase 39-40: Agency Foundation & Connection Management |
| 3.21 | 2026-01-21 | Panorama | Phase 38.2: Admin Conversations & Quick Prompts |
| 3.20 | 2026-01-21 | Horizon | Phase 38.1: User Admin Bug Fixes |
| 3.19 | 2026-01-19 | Chronicle | Phase 38: Research Studio Enhancements |
